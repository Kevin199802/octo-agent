# Roadmap — octo-insight 文档维护

> 本仓是 insight 设计文档库,本 ROADMAP 跟踪**文档维护**任务,只面向未来待办。
> insight 的功能 / 代码开发计划在 UXAI 仓。
> 找某个 `SPEC-INS-NNN` 对应哪个文件,查 [docs/specs/README.md](docs/specs/README.md) 登记表,不要在本文件里全文检索。
> 已完成的实现里程碑史料见 [README.md「参考旧实现」](README.md#参考旧实现);UXAI 侧版本改动记录见 [docs/releases/](docs/releases/)。

**规模标签**:`[S]` < 半天　`[M]` 1–2 天　`[L]` 3–5 天

---

## 待办

| 规模 | 任务 | 说明 |
|---|---|---|
| `[L]` | **fastui/lake 组件代码生成:预览与交付管道(Design)** | spec [SPEC-DES-001 fastui-vue-codegen-pipeline](docs/specs/design/fastui-vue-codegen-pipeline.md)(草案 v14,**UXAI 侧改动收口至只剩 bridge 监听端;首装链路 2026-09-08 在第一台没装过 node 的 macOS 上炸出四处,已修五项(PR #21),余 S2/S5/S8**);内网托管操作手册与 UXAI 侧五件已从主 spec 拆出为 [SPEC-DES-002 fastui-env-hosting](docs/specs/design/fastui-env-hosting.md)(给内网运维照着做) / [SPEC-DES-003 fastui-uxai-integration](docs/specs/design/fastui-uxai-integration.md)(给 UXAI 前端),小节号沿用。让设计师用自然语言生成基于内网 fastui/lake 组件库的 `.vue`,在**真实脚手架**里编译渲染,交付开发可直接 `yarn install && yarn serve` 的工程。**不做轻量模拟预览**——组件与内网微组件框架深度耦合,`yarn build` 产物 89MB 单独打开仍卡在微前端 loading,浏览器内 SFC 编译 / mini bundler / 全局安装 / 改 `resolve.modules` 全部因此否决(spec §1 逐条记录理由,避免重复推演)。**机制:共享依赖池 + 依赖外置**——1GB 依赖每台机器只装一份;链接建在**会话根**(outputs 的父级)而非工程内;直连 cli-service 启动绕过 yarn 的 `.bin` PATH 与 shim 写死层级两道坎;`turboui.config.js` 三处环境变量注入全部带回退,开发拿到交付包无需改回。于是**产物目录零链接**。**已实测**:webpack 跨两层解析(209/227 modules)、两个环境变量均生效、断网可渲染、Windows junction 普通用户可建;**预览链路零改动**(`<artifact type="text/link">` → 卡片 → iframe,内网已验)、运行时错误 bridge 已验。**已完成**:skill 第一版(`skills/fastui-vue-creator/`,四脚本 + 两版安装脚本 + SKILL.md)与本地 V0(并发端口原子占位、产物零链接、升级链路可触发、detached 存活、编译失败原文回传)。**UXAI 侧五件已收口四件**([SPEC-DES-003](docs/specs/design/fastui-uxai-integration.md),属 Design 模块走 PR 协议):dev server 宿主化(PR #801,内网实测通过)、导出代码包按钮 + 预览就绪时序(PR #812,**待内网实测**)、external URL tab 编辑类功能 gate(查明不用改——`url` 的能力表早已全关)。**skill 侧首装修复**(spec §0.0 S 表):S1 代理强制直连(含 npm/yarn 子进程摘掉代理变量)、S3 删除护栏(内网已因 mklink 乱码误判导致一次用户数据永久丢失)、S4 macOS 首装必挂的 yarn 路径 + `shell:true` 空格、S6 registry 取值挂在 skip-node 分支、S7 `--upgrade` 拼给 PowerShell 从来没生效、S9 ps1 的 Fail 定义在调用之后致裸崩 —— **六项已修(PR #21,经两轮 review)**;**🚫 另发现服务端阻塞**:macOS 的 node 包在 nginx 上 403(Windows 包正常),代码侧绕不过去,需内网投放侧确认;**待**:S2 doctor 探针重做(阻塞于 §10 Q10)、S5 日志落盘只有契约行(子进程原文全丢)、S8 安装脚本的裸崩路径打不出契约行;运行时错误 bridge 的宿主监听端(等第三层做);验证缺口是 Intel Mac,以及首装必须由 agent 全程跑(人在终端复现不出代理问题)。**范围外**:二次编辑回环 |
| `[S]` | **@技能激活的服务端事件上报** | spec [SPEC-INS-029 insight-skill-activation-event](docs/specs/infra/insight-skill-activation-event.md)（草案）。修「内网全局 `skill.used` 监听器统计不到 insight 的技能调用」：insight @技能走 [SPEC-INS-023 §2.2](docs/specs/ui/insight-mention-at.md) 的 3b synthetic 注入，**完全不经服务端的技能概念**，两个 `skill.used` 发布点（模型调 `skill` 工具 / 走 `session.command`）都不触发——这是 023 写明并接受的代价，不是缺陷。首个修法 [UXAI #578](https://github.com/MyHeavenDyf/UXAI/pull/578)（`/skill` 改走 `session.command`）已关闭：覆盖面错位（insight 入口是 `@` 不是 `/`）、整篇 SKILL.md 会显进用户气泡（上游 `SessionTurn` 取第一个非 synthetic text part，正是 023 §2.2 绕开的坑）、丢 turn 级 tools gate（`CommandInput` 无 `tools` 字段，而它是 session 级 permission 靠每轮覆盖）、`session.command` 同步阻塞整轮。改走 3c：前端随 `promptAsync` 传 `extra.skills`（上游现成字段，studio 已在用），服务端 `prompt()` 在权威侧 publish——落点在 service 层，不受「Hono vs Effect HttpApi 两套后端」影响。约 20 行 / 5 文件，气泡、tools gate、发送时序全不动。**待**：UXAI 分支实现 + 外网 V1–V12（含 SSE 事件流断言）+ 提 PR；事件体缺 `sessionID`/`source` 致无法按 module 归因的问题记在 029 §6，需先与监听方对齐字段口径再单独修 |
| `[M]` | **会话工作目录声明对齐（修 skill 产物散落）** | spec [SPEC-INS-028 insight-workdir-declaration](docs/specs/infra/insight-workdir-declaration.md)（**实现已合入 dev**）+ 对外契约 [artifact-output-for-skills](docs/specs/agents/artifact-output-for-skills.md)。修「接入外部 skill 后产物随机散落在选中目录根与 outputs」：**散落不随机，是按写盘通道确定性分裂**——`write` 相对路径走插件重定向进 outputs，`write` 绝对路径与 **skill 脚本经 bash 落盘完全不经过重定向**，落选中目录根。再往上追，根因是全系统只有一处目录声明（`Instance.directory`，`session.directory` 只是建会话时抄的一份副本），`system.ts:55` 把它作为 `Working directory` 声明给模型，而插件**只改了行为没改声明**；模型拼出的错误绝对路径字符串上就等于我们声明的值——是执行了声明，不是遵从度问题。2026-07-30 定的四条落法（learning §6）一条都没动过声明。做法：借上游现成的 `Working directory` / `Workspace root folder` 二元语义 + `experimental.chat.system.transform` hook（带 sessionID，本仓 `proto-theme.ts` 已有先例）把声明改成会话产物目录，bash `workdir` 默认值 / read / glob / grep 基准一并对齐，插件从"唯一手段"降为确定性兜底；顺带放开产物子目录（服务端 list 与文件管理 `navigateToFolder` **早已支持**，只被提示词锁死）并补 containment 校验。**已完成**：§6 落地清单 5 项全部合入 dev（插件更名为 `octo-session-workdir.ts`，旧 `octo-outputs-redirect.ts` 已删；提示词已降级）。**待**：外网 V1–V11 + 内网 N1–N4（真实 skill 全流程）走查记录；另立 learning 记「Working directory 与 Workspace root folder 的区分」并给旧笔记 §6 收尾。**2026-08-21 spec v2**：§3.2.3 推翻 v1 的「声明层不碰磁盘 / 否决 eager」——实测模型会**探测**声明里的目录，而探测走的是绝对路径 / 显式 `workdir`，执行层对这两者早返回、不 `ensureDir`，探测必然扑空，模型转而逃向 `/tmp`（UXAI PR #682）。根子是 v1 把 `outputs/` 归错类（当成「用户指定的既有目录」，实为「程序为本次会话造的专属目录」，systemd 两类目录的相反行为是判据）。现行：声明层与执行层都建；补 V4.2 / V4.3 |
| `[M]` | **会话排队 drain 运行器（UI 无关 · 跨模块通用）** | spec [SPEC-INS-027 session-queue-runner](docs/specs/ui/session-queue-runner.md)（**实现已合入 dev**）。修「busy 期间排队时切到 `/skills` 路由页或相邻 agent tab → 会话后台跑完、排队却卡死」：**根因是架构缺层**——队列数据已提到模块级，但 drain 触发器仍留在 insight 页面组件内（edge-triggered `on(isBusy,…,{defer})`），页面一卸载触发器被 dispose、idle 边沿没人听。切对话能救是因为那是同路由换 `params.id`、页面不卸载、补偿 effect 触发。业界 agent（Claude Code/ChatGPT/Slack 草稿队列）一致把 drain loop 放数据层、按会话生命周期事件驱动、后台照常推进。做成**应用根（`GlobalSyncProvider` 内）常驻、UI 无关、level-triggered + per-session in-flight 守卫**的全局 runner；队列 per (module,sessionID) 分桶、每模块各持一份，天然隔离。**与 [SPEC-INS-024 composer-draft](docs/specs/ui/composer-draft.md) 同构**（页面态提到脱离视图层）。insight 先接入，其他模块按 spec §5 接入指南复用同一 runner 内核（chat/Design 走上游 followup 的不改，只给模式参考）。**已完成**:全局 runner（`pages/insight/queue-runner.tsx` + `octoapp/utils/session-queue-runner.ts`）与 insight 接入均已合入 dev，页内两处 in-page flush 触发器已移除。**待**:其他模块按 spec §5 接入 |
| ✅ `[S]` | **出卡机制修正:退役路径 C + 任务查询只在 completed 那次出卡(已落)** | [output-renderers §0.1/§2.6](docs/specs/ui/output-renderers.md) + [task-card §入口冗余](docs/specs/ui/task-card.md)。**问题1**:模型写脚本(`gen_word.ps1`)再执行生成 docx 时,脚本被出卡、真交付物 docx 抓不到反不出卡(§2.6.1 已知边界 1+2 组合)→ **退役路径 C**,write 产物统一交「文件管理」面板(独立扫盘+预览),不再靠启发式分辨过程产物 vs 交付物;「write 完成→文件管理刷新」解耦成独立 effect 保留。**问题2**:`get_task_result` 重复查询时每一次「处理中」查询回答下方都被回填最终产物卡 → gate 在本 turn 真正观测到 `completed` 时才回填。**已落 UXAI PR #384**(typecheck/33 单测过,**端到端待内网联调**)。**后续**:真交付物(脚本产出 docx/xlsx)出卡根治属契约层——脚本执行后显式声明产物路径,见下方待办新增项 |
| `[S]` | **`@` 引用的图片接上 vision 通道** | [SPEC-INS-015 §1](docs/specs/infra/insight-file-passing.md) 要点第 3 条的遗留：`@` 引用的会话内图片当初不走 ③，理由是「vision 需要 S3 url，而 `@` 的本地图片没有」。③ 去 S3 后（[ADR-017](docs/adr/017-insight-image-local-path-base64.md)）这条理由**已不成立**——新链路要的正是本地路径，`@` 的会话文件天然就有。做法：把 `@` 图片并进 `imageFiles` 分流即可，同时套用 §4 的图片大小上限（`INSIGHT_IMAGE_MAX`）与 mime 精确兜底（`imageMimeFor`）。**前置已就绪**：UXAI #754 已合入 dev（`993e64bae`），`file://` 图片链路可直接复用。**待**：UXAI 侧实现 + 补一条「`@` 图片 → vision FilePart」的单测 |
| `[S]` | **契约层:脚本执行产物的显式声明出卡** | 承接上一行退役路径 C 遗留的假阴性:模型用 `bash`/`powershell` 执行脚本产出的 docx/xlsx 等真交付物,从 tool part 抓不到(从 shell 输出扒路径太脆弱,§2.6.1 已知边界 2)。根治要**脚本执行后显式声明产物路径**(走 MCP resource_link 或专门约定),而非客户端嗅探。**待**:定契约形态 + 决定是否值得做(当前文件管理已能兜底找到) |
| `[M]` | **insight 会话列表服务端分页** | spec [insight-session-list-pagination](docs/specs/ui/insight-session-list-pagination.md)（SPEC-INS-013,草案）。修「会话超 100 条后最早的看不到」:新建 **insight 专用** server 端点 `/insight/sessions`(独立成组照 studio 样板,硬编码 `agent=octo_insight` 服务端过滤 + `{items,total}` 分页),前端 `session-list` 切到新端点 + 加载更多。承接 [session-agent-attribution §10](docs/specs/infra/session-agent-attribution.md) 预留项。**待**:服务端走 intranet-handoff 非绿灯流程落地 + SDK 重生成(后端先于前端);性能(复合索引 / 游标 / 虚拟滚动)归后续阶段 |
| `[S]` | **弱模型工具意图识别评测** | 预置文案 2026-06-15 去掉明示工具名后(SPEC-INS-007 §3.1.2),"选对 tool"压在弱模型 + 提示词上。按 [SPEC-INS-007 §11](docs/specs/ui/insight-prompt-redesign.md) 做 `expectedTool` 对账评测(按钮命中率 ≥95%、自由输入 ≥85%),不达标按 §11.3 升级阶梯回退。区分"选错工具"与"文件拆桶错"两类错误。**待**:内网真机评测数据。**2026-07-03 注**:SPEC-INS-017 MCP 按钮化后,MCP 工具退出隐式选择,评测范围收窄到本地工具,负担变小 |
| `[M]` | **内网知识库问答迁入 insight + chat 下线（chat→insight 合并）** | spec [SPEC-INS-030 insight-knowledge-search](docs/specs/agents/insight-knowledge-search.md)（草案，supersede 旧 [chat-knowledge-search](docs/specs/agents/chat-knowledge-search.md)）+ learning [rag-chat-integration](docs/learning/rag-chat-integration.md)/[rag-mental-model](docs/learning/rag-mental-model.md)。产品决策把 chat 与 insight 合并、chat 下线;chat 唯一独有能力=知识库问答,迁入 insight。**旧 V1 代码已落 UXAI**(knowledge_search 工具 + registry 网关 octo_ai + octo_ai prompt + base_url 桥 + mock,typecheck 过、未 commit)。**合并结论(spec §0)**:工具网关 octo_ai→octo_insight + 引用 UI 在 insight-turn 重建 + 弃用 octo_ai.txt/octo_insight 补 knowledge_search 段 + account 限流必修(真实工号,`localStorage.userInfo.account` 纯工号) + 历史降级兼容(不报错/不丢原文)。**新老接口都在**(新接口 `queryKnowledge`,body 只发 `{question,account}`+扁平响应,spec §4)。**执行拆分**:提示词(PR-A)/account(PR-B)不依赖 Q3 可先行;合并主体(PR-C)先落旧接口;新接口切换(Q3,PR-D)另做(**Q3 已于 2026-08-22 定案,见下**)。**2026-08-12:PR-A/B/C 已提 PR**([UXAI #633](https://github.com/MyHeavenDyf/UXAI/pull/633) 提示词+account、[#634](https://github.com/MyHeavenDyf/UXAI/pull/634) 合并主体,**须按序一起合入**;全仓 typecheck 绿)。落地拍板已回填 spec:account 走 `promptAsync.extra` 而非 synthetic part;`octo_ai` agent 保留只丢 prompt;**历史迁移推翻路径 B(读时合并)**——它只解了 agent 一半,chat 列表当年是 `scope=project` 跨目录全展示、insight 按 directory 严格过滤,放宽后仍是半可见,改走显式迁移(→ SPEC-INS-031)。另确认:旧接口不传库名 = 后端顺序遍历、**效果明显更差**,PR-C 这版只是链路跑通不等于可用。**Q3 定案(2026-08-22):只查「全量库」、不做多库路由**——IT 已把 15 模块库并成单一合并索引 + 统一 rerank,新接口 `queryKnowledge` **不传 `knowledgeName` 即走全量库**(后端已兼容);这推翻了早先"跨库无分数可比 → 多库只能选定一库查一次"的结论,库描述/意图路由/注册表/客户端重排整套不做,"Q3 库清单来源"确认项随之作废。返回条数当前固定 5、已按文档去重(够不够走 N6 内网验)。**待**:内网跑通知识库问答(引用可点 / 引用列表 / account 生效)+ 新接口切换(PR-D:不传库名走全量库 + 扁平 parse + mock 同步) |
| `[S]` | **Chat 历史会话迁移(chat 下线收尾)** | spec [SPEC-INS-031 insight-chat-session-migration](docs/specs/infra/insight-chat-session-migration.md)(草案)。承接 SPEC-INS-030 §6.1:chat 历史(agent=octo_ai)在 insight 列表不可见,且 chat 会话**本来就没有目录归属语义**(`scope=project` 丢掉 directory 条件),故不是"还原归属"而是"安一个家"。做法:设置 → 通用 → Proxy 下方加一节,文件夹选择器(默认当前选中目录)+ 迁移按钮,一次性回填 `agent` + `directory` + **`project_id`**(三者缺一不可——列表是 project_id 与 directory 同时过滤,漏写 project_id 会"提示成功但列表为空"的静默失败;project_id 与目录是**多对一**,非 git 目录一律 `global`)。**不建表不加 schema**:迁移前静默备份整库,**备份文件即迁移记录**,重新迁移直接从备份读 octo_ai 的 id 集合;不做还原按钮(整库还原会连带抹掉迁移后的新数据,而唯一现实的后悔场景"选错目录"用重迁覆盖)。**临时功能,后续版本整体下掉**。**待**:内网先跑 spec §6.3 那条 SQL 量历史分布 → 实现(接口走类型化 Effect HttpApi 的 insight 分组,**不是 Hono**)+ 外网 V1–V13 + 内网 N1–N4(含承接自 030 的真实历史底线验证) |
| `[S~M]` | **insight question 工具答题 UI** | spec [SPEC-INS-025](docs/specs/ui/insight-question-dock.md)（草案）。现状取证（2026-07-28 实测修正,初稿结论已推翻）：`question` **根本不在模型工具列表里**——`agent.ts:124` 的全局 `defaults` 写着 `question: "deny"`(只有 plan agent 显式 allow),`octo_insight` 继承了它,`Permission.disabled` 把工具整个摘掉;现象是模型回「我并没有名为 question 的工具」,而**不是**初稿以为的「调用后卡死」。故须**权限 + UI 一起做**:①`agent.ts` 的 octo_insight 加 `question: "allow"`(实测 `debug agent` 前 false / 后 true);②照 `permission-dock` 自包含先例 port `question-dock.tsx`(用 `<DockPrompt kind="question">` 白嫖上游 `message-part.css:953` 样式,跨页只 import 纯函数 `sessionQuestionRequest`),交互对齐 Claude AskUserQuestion(分页 / 单选多选 / 自定义答案 / Skip)。**硬约束**:答案不做词组胶囊(上游本就整行铺开,仅需在自己 css 补 `option-label` 的 `overflow-wrap`,已实测生效)。顺带修 web 入口 `app.tsx` 漏挂 `insightDevRoutes()`(dev 预览页此前浏览器/Playwright 够不到)。**范围外**:其他 agent 的 question 权限由各页同事自行放开(这条全局 deny 对 design/studio 同样生效)、不动 octo_insight 提示词(调用引导随知识库线另出)、不动 packages/ui。**已实现(外网)**:typecheck 双包过;V0 权限/V1 交互/V3 长答案/V6 共存 Playwright 实测通过;**待**:V2 真实链路端到端 + V4 路由切换 + V5 Skip,内网验证 |
| `[L]` | **文档视角迁移到 UXAI** | architecture / development / 各 spec / ADR 里的代码路径、开发流程逐篇调成 UXAI 视角(路径映射见 [handoff §0](docs/intranet-handoff.md)),让文档直接服务 UXAI 开发,而非要求读者心算映射。执行清单见 [spec docs-uxai-perspective-rewrite](docs/specs/infra/docs-uxai-perspective-rewrite.md)。**进度**:✅ architecture.md(全文 UXAI 化 + 删本地壳台账 §5.4);⬜ insight-debugging / development / 各 spec / ADR |
| ✅ `[S]` | **markdown 卡编辑 spec(已落)** | [insight-markdown-editor.md](docs/specs/ui/insight-markdown-editor.md) 已立:**Vditor**(Arya 同款) `sv` 全屏左右分屏、自动保存(防抖)落盘本地 projectdir 文件(跟随 useProjectDir,不走 MCP;云端回写后续走 API)、新增 `writeFile` IPC、Vditor 资源本地化。**P1 实现已落 UXAI**(分支 `feat/insight-markdown-editor`:writeFile IPC + Vditor 接入/资源本地化 + 全屏 overlay + 自动保存/还原 + 主题跟随;typecheck/build/单测过,**断网资源验证 §8E 待内网真机**)。P2(模式偏好持久化 / Cmd+S flush 已附带)。**2026-06 持久化修复(§3.5)**:`downloadResourceToTemp` 改幂等(此前每次 re-fetch 覆盖 → 本地打开/编辑改动被 MCP 原版盖掉)+ uri markdown 卡预览改读本地工作副本 + 下载菜单改「另存为」(始终拉 url 原件)。typecheck/单测过,**待内网真机验证**。背景:[output-renderers §2.5.2](docs/specs/ui/output-renderers.md) markdown 卡 + docx→md([mcp-contract](docs/specs/agents/mcp-contract.md)) |
| `[M]` | **insight 输入框 `@` 引用面板** | spec [insight-mention-at](docs/specs/ui/insight-mention-at.md)（SPEC-INS-023,**已实现(外网),待内网验证**）。参考站内 Design（make PR #428）的 `@` 面板做本地化适配:只做**技能库**（`octo_insight`+`common`）与**会话文件**引用。insight 原用纯 `<textarea>` + `promptAsync`（Design 用 ProseMirror + `session.command`）,本项已把输入框换成 ProseMirror 行内胶囊;技能落地选**方案 3b**——自读 SKILL.md 作 synthetic part 注入（气泡不显、确定性进上下文），规避上游 `SessionTurn` 不认 `displayText` 致 3a 需重写用户气泡的成本。文件走文本引用 `读取{path}这个文件`。queue 改结构携带 skills 防排队丢技能。**已合 UXAI**:PR #432(主体,typecheck 过) / #481 / #562(技能胶囊补配对空格,2026-08-04)。**待**:内网真机验证(技能确定性激活 / 文件 `extract_document` 读取 / 排队带引用 / 中文输入法 + 胶囊交互回归 / 文件管理实时同步 / 打点上报);另有两条已知未修问题(`insertedThisRound` 跨轮残留、句中插入双空格)见 spec §11。**2026-08-20 内网修复 `@文件` × chip 的契约漏配**(spec §8):`[附件]` 曾是 MCP 文件白名单的唯一载体,于是「先对话生成 md → `@` 它 → 选研究工具」必死(模型按铁律自我阻断 / 插件 `resolvePath` miss 抛错);修法是 `octo-upload-inject` 的 `MANIFEST_HEADERS` 同收 `[附件]` / `[引用文件]`,chip 模板与常驻提示词措辞改为「会话文件清单」,白名单语义(只认清单内、精确匹配、miss 响亮失败)不变。**2026-08-20 同日修 `@` 与附件的内联不一致**(spec §7.2/§8):`@` 的文件改为与附件走同一条 FilePart 内联路径(回归上游机制——Cursor/Copilot/Claude Code 与 opencode 自身的 `@` 都是发送即注入上下文;「给路径让模型自己读」服务的是 agent 探索大仓库,不是用户显式引用);内联判定同时从正向白名单 `{txt,md}` 改为**反向排除**(只排 office/pdf/图片,其余交服务端 `read` 判二进制),上传格式放开时不必再同步一次。chip turn **不特殊处理**(起草时计划关闭内联,核实后撤回:内联只涉文本类且有 50KB 截断,§8-9 的炸弹源头是 `extract_document` 对 office 全文回灌;而 chip 纯常驻,关掉会让用户选中研究工具期间对文件内容彻底失明) |
| ✅ `[S]` | **输入区草稿保留（per 会话分桶 · 跨模块通用，v1 已落）** | [SPEC-INS-024 composer-draft](docs/specs/ui/composer-draft.md)。修「切 agent（Chat/Make/Studio）与切会话时，输入框内容 + 附件 + MCP chip 全被清空」。**根因是架构缺层**而非漏写保存:insight 把输入态放在页面组件 signal 里,`/insight/:id?` 换 id 跨不过去、切顶层 tab 整页卸载就销毁;上游 chat 不丢是因为它有 `context/prompt.tsx`(per `(dir,sessionID)` 分桶 + LRU + 落盘)。业界(ChatGPT/Claude/Slack/Linear/VS Code/opencode)一致是「per 会话分桶 + 脱离视图层存活」,无一家「切走就清」。**做成跨模块通用能力**(`octoapp/utils/composer-draft.ts` 存储层 + `octoapp/hooks/use-composer-draft.ts` 适配层),insight 先接,make/pattern/studio 后续复用同一 hook。刻意**不**复用上游 `persisted()`——它要 `usePlatform()`+owner 故必须 Provider 化、桌面端还是异步存储(要 ready 门闸、有覆盖用户键入的竞态);改用同步 localStorage(与 make prompt 草稿 / chat-width 同款,已验证),换来 import 即用、无门闸、无首帧闪烁。落盘编解码由**页面**给(只有页面知道哪些附件态跨得过重启):insight 只落 done 态元数据 + 正文 + chip 的 presetId,previewUrl 按 path/S3 url 重建。三个真时序问题(不是搬代码):①上传中切走的回调写回归属(否则附件永远转圈 + 锁死发送键)②首次发送在 navigate 前整桶改名(顺带删掉 `sendingNavigation` 补丁 flag)③objectURL/File 引用生命周期显式管。LRU 20 **按 scope 独立计**。**已落 UXAI 分支 `feat/composer-draft-per-session`**(typecheck 0 / 328+489 单测过 / vite build 过;**实机验证待内网**,清单见 spec)。顺带修 CI 缺口:`test:unit`/`test:ci` 原先只跑 `./src`,octoapp 全部单测(含 insight 既有十余个)从未进 CI——因两棵镜像树同名文件 + bun `mock.module` 进程级污染,拆成两次独立进程跑 |
| `[S]` | **产物下载修复的收尾** | 承接 UXAI PR #344(主进程下载换 `net.fetch`,已合入并内网实证生效;机制见 learning [electron-main-fetch-vs-net-fetch](docs/learning/electron-main-fetch-vs-net-fetch.md))。**待**:①**PR #349 待合**——console 对象序列化(生产构建装、dev 不装,`pages/insight/lib/console-serialize.ts`)+ 三个 TabBody 重试加载态 + 复制链接 toast;读旧版本用户日志仍是 `[object Object]`,版本收齐后可删相关文档提醒。②**观察项**:sidecar 上传 `octo-upload-inject.ts` 仍是 undici + env 代理(内网实测通),其 catch 只取 `e.message`、`cause` 同样被吞——哪天报"上传失败但浏览器正常"就是它,届时一并修。③已报给用户、按用户决定**不动**的同类坑:excode `ResolveIcons.ts` 主进程 undici fetch 图标 API(PAC-only 机器会挂,归 Design/excode 负责人);sidecar `internel_image_generate` / `knowledge_search` / jimeng 同风险面 |
| `[S]` | **MCP 传参三 bug 的统一回归** | 用户 2026-07-03 定的安排:**回归推迟到 [SPEC-INS-017](docs/specs/infra/insight-mcp-explicit-entry.md) 实现 + S3 整改(上传合同 v2)完成后统一做**,不逐个修逐个测。上传合同 v2(uuid key + 自有域名下载 + `Content-Disposition`,文件名退出 URL)提案见 [file-upload.md](docs/specs/infra/file-upload.md) 顶部,**待与后台对齐**;v2 落地前特殊字符/撞名文件名走 MCP 失败属**已知窗口**,客户端清洗已全部下线、不再补防御 |
| `[S]` | **设计稿截图 → HTML 生成服务：网关能力验证** | spec [screenshot-to-html](docs/specs/infra/screenshot-to-html.md)（草案，非 insight 专属故不占 SPEC-INS 号段）。内网 web 端从设计稿生成截图 → 空闲时段批量喂多模态模型还原成 HTML，Java 后台实现。**核心决策已定**：前端→本服务契约**永远传 url**（图本就在上传服务里），本服务→模型网关用 URL 还是 data URI 是**内部实现**，一个配置项 `imageMode` 切换、前端零感知——把「网关吃不吃远程 URL」这个未知从卡设计降级成选一个配置值。URL 优在服务端零内存 + 请求体可留档（base64 峰值 ≈ 原图 ×4~5，是批处理 OOM 主因；日志留不下就无法复现坏结果），但 URL 也有代价：多一跳不在我们控制内、时序耦合（排队久 + 清理任务 = 404）、SSRF 评审，故 `DATA_URI` 是平级退路不是降级。**2026-08-14 认知修正（连带修了两处文档硬伤）**：图片的 base64 **不进 tokenizer、不占上下文窗口**（走 vision 解码通道，image token 数只由分辨率决定，URL 与 base64 完全相同），所以「用 base64 会更费 token / 跑不出结果」不成立，`DATA_URI` 在**生成质量上零损失**，不值得为切 URL 去申请放开网关；旧 learning [file-passing-to-models](docs/learning/file-passing-to-models.md) 写的「5MB base64 ≈ 170 万 token 装不下」只在 base64 被当 **text part** 时成立，已修正，[ADR-015 决策 2](docs/adr/015-file-passing-architecture.md) 引用该错误理由的部分同步换成真实理由（跨进程边界 + 内存 / 请求体 / 日志留痕），**决策结论未变**。唯一真实差异源是「谁做的缩放预处理」（spec §3.4），切模式时要对齐。**待**：把 spec §4 验证脚本（Python 标准库，含 T2 对照组 / T1 主用例 / **T3 不可达 URL 哨兵**——防「网关静默忽略图片、模型瞎编」的假通过）发内网同学跑 N1，据结果定死 `imageMode`；再确认模型网关是否 OpenAI 兼容、被测地址与生产是否同一个（内网常见统一网关与直连推理并存） |
| `[M]` | **learning 笔记补全** | SQLite / Drizzle / Effect / Solid 响应式 / IPC / SDK 生成机制等,沉淀给未来 |
| `[S]` | **ADR 体系梳理** | 已落地但未立 ADR 的决策补全(session.agent 字段化、useProjectDir 全栈抽象等);交叉引用清理 |
| `[S]` | **specs 现役 / 历史分层** | 运行一段时间后看是否需要按"现役 / 历史"分子目录 |
| ✅ `[S]` | **SPEC-INS-009 收起入口迁移同步** | [insight-result-panel-reveal.md](docs/specs/ui/insight-result-panel-reveal.md) §3.3 / §4：收起按钮由 TabBar 右侧迁至 ConversationHeader「文件管理」按钮（inline 不传 `onCollapse` → TabBar 不渲染收起按钮；窄屏抽屉态 TabBar 保留关抽屉按钮）。代码见 UXAI `feat/insight-icons`（PR #523） |

---

## 本地解析能力线（spec 序列 · 独立分支逐个上线）

> 背景：MCP 解析由他方团队提供，现新增"本地解析线"与之**并行解耦**（团队责任一目了然、用户能说清哪个更好用）。核心是把本地文件系统升级为真相源：源文件 + 产物落本地 → @引用 / 二次生成 / 本地解析在本地完成；MCP 路一行不改，只多"我们这侧下载结果到本地"的纯消费动作。
>
> **原则**：相对独立的先做；**每份 spec 单独拉分支、单独上线**，不互相阻塞。测量机制寄生在 extract（免费返回字数），护栏**策略**跟着"本地解析能力"走（第一个真消费者），不提前、不单开 spec。

| 序 | 规模 | spec / 任务 | 依赖 | 状态 |
|---|---|---|---|---|
| A | `[M]` | **本地工作目录布局**（worktree 文档本地化）[SPEC-INS-014](docs/specs/infra/insight-worktree-layout.md) — v1：`insight/sources`(拷贝落地) + `insight/outputs`(扁平)、产物落点 `.octo/downloads`→`insight/outputs` 迁移，**已合入 dev**（PR #238）。**v2（2026-07-08 修订）**：反转 v1 的"projectDir 扁平共享"决定，改为**按会话隔离** `insight/<sessionId>/{uploads,outputs}`（`sources`→`uploads` 改名，对齐 Claude / 站内 Make 模块）+ 预会话落地区 `insight/uploads/`（发送时 rename 进真会话）+ 配套文件管理 UI（§10，取代 [SPEC-INS-004](docs/specs/ui/insight-workspace.md) 原草案侧栏设计）。**§10.1（v3，2026-07-09 已实现）**：文件管理 UI 对齐站内 Design 模块——表格视图（多选/表头排序/按类型或修改时间分组/类型筛选）+ 真上传（文件选择器 + 拖拽，复用附件 copy→move 链路）；kind 分类走客户端派生（未动服务端）；连带修 viewMode 打开产物 tab 不聚焦的 §10 回归。**v4（2026-07-14 修订）产物落盘机制**：确立「文件管理 = 会话真产物库」，MCP `uri` 产物改**出卡即落**（eager；此前 uri 卡只 markdown 点开才落、其余只 fetch 不落盘 → 思维导图等产物不进文件管理）；inline 嗅探卡明确不物化落盘、`write` scratch 产物维持不落（均避免污染产物库）；下载/另存不双写。**v5（2026-07-18 修订）**：`write` 产物落点从 v4「提示词约定写 outputs」改为**服务端确定性重定向**（相对 filePath → 会话 outputs/，插件 `octo-outputs-redirect`，两道确定性闸门 `tool==="write"` × `session.agent==="octo_insight"` 只影响 insight write、不动上游 write 本体）；删除每轮注入的 `[输出目录]` synthetic 指令（内网弱模型会复述它、把 outputs 绝对路径暴露给用户）；实证 scratch 走 `TRUNCATION_DIR` 不经 write，故 write 相对产物一律落 outputs 无污染。**v7（2026-07-22 修订）本地落点根统一**：按 PM 全局约定把落点根从 `insight/` 迁到 `.octo/`、**去掉 agent 命名层**（`insight/<sessionId>/`→`.octo/<sessionId>/`，agent 归属由 sessionId 反查）、预会话区 `insight/uploads/`→`.octo/tmps/`；反转 §2「不藏 .octo/」旧决策；会话段 `<sessionId>` 即平台 `session.id`（形如 `ses_…`），与 make `.octo/artifacts/make/<sessionId>/` 同源；存量本地文件不迁移。**v7.1（2026-07-22 修订）outputs materialize 幂等持久化到磁盘清单**：内存表跨重启/重装清空致重开旧会话把同名产物重落 `xxx (2)`（#90 重装后产物重复），把幂等键（仍是资源 URI）从内存表搬到磁盘持久清单 `.octo/<sessionId>/outputs/.materialized.json`（`URI→{file,fetchedAt}`，dotfile 不进文件管理，随会话目录活过重装），命中且文件在即复用、未命中才 `collisionFreePath` 落盘写回；内存 Map 保留为快路径（键加 `outputsDir` 前缀防跨会话串场）。按 URI 记身份不按文件名（filename≠identity，起草的「确定性按名复用」经评审否掉），uploads/tmps 仍加后缀。**v7.2（2026-07-24 修订）搬迁判据健壮性**：v7 迁移漏改渲染端搬迁判据（`isPendingUploadPath` 仍找旧 `insight/uploads` → 恒假 → 附件搬不进会话、文件管理为空，PR #424 止血）；收敛为布局知识单一入口 `worktree-layout.ts`（判据抽出可单测）+ ipc.ts 交叉引用注释 + 新增 §2.1 实现映射表（改布局须 spec/ipc.ts/worktree-layout.ts 三处同步），并把文件管理 refresh 从耦合搬迁判据（`movedPaths.size>0`）解耦为 gate 有无本地附件（`localFiles.length>0`）；复盘见 learning [stale-path-predicate-after-layout-refactor.md](docs/learning/stale-path-predicate-after-layout-refactor.md)。**v8（2026-07-30 修订）去掉文件管理的预览面板第四栏**：清掉 7-18 `dc02357d8` 一次**未登记的实现漂移**（引入 `PreviewPane`——单击文件行开右侧第四栏、内容自己走 `/artifact/content` + base64 data URL 渲染，未进 §10.1 对照表与落地清单，于是「预览产物」在系统里有了两套实现），**回归 §10（v2）原始决定「单击文件行直接以 tab 打开」**；打开后的分流交给已有的唯一入口 `resolveOutputType`（[SPEC-INS-026](docs/specs/infra/insight-artifact-identity.md) §4.2），故这不是新增白名单而是删掉一套重复实现。两个缺陷同时消失：① md 产物「白屏 + 再点一次才正常」——面板 md 分支只把源文本塞进空的 `prose` class 且未设 `color`，所谓「再点就好」是点中面板上那层 `absolute inset-0 z-10` 透明蒙层触发 `openTab`、看到的其实是 tab 的渲染结果；② pdf/docx/pptx/xlsx/csv/zip/psd 在面板里全空白——原面板 `Switch` **缺 default 分支**（新实现靠 `resolveOutputType` 的 `file` 档兜底进 FileFallback 中间页，比改造前更好）。另修 `handleDownload` 改 IPC `readFileBuffer` 优先（原直连 `/artifact/content`，其底层 `File.read` 对文本 `.trim()`，下载的文本文件尾部换行被静默吃掉）。**音视频应用内预览拆为独立第二步**且明确**不沿用 Design 的 base64 data URL**（膨胀 33%、整份进内存，几百 MB 访谈录像不可行），需先给 `local://` 协议补 Range 流式 + 补 mime 表（`.mov` 缺失）+ 在 026 显式修订「类型集收敛后不再增减」那句；本期音视频走中间页用系统播放器打开 | 无（建于 SPEC-INS-012 projectDir 之上）| **v2 核心链路已实现并内网验证通**（UXAI `feat/insight-session-scoped-files`：会话隔离存储 + 发送时 rename + 文件管理面板 + 服务端接口，`extract_document` 端到端跑通）；**v3 UI 打磨已实现，待内网验证**；**v4 eager 落盘已实现**（UXAI `fix/insight-eager-materialize-outputs`：index taskCards effect + insight-turn 路径 A effect + `materializeUriCardToOutputs`；typecheck/lint/328 单测过，**待内网验证**）；**v5 落点重定向已实现并内网验证通**（UXAI PR #368：发「你好」不再冒路径 + 生成文件落 outputs 可见，用户实测）；**v7 落点迁 .octo 已实现**（UXAI PR #411：8 处落点 + write-file 白名单改按 .octo 分段；opencode/desktop/app 三包 typecheck 过，待内网验证）；**v7.1 outputs 按 URI 持久清单幂等已实现**（UXAI PR #418：`downloadResourceToTemp` 幂等键=URI、落地映射持久化到 `.materialized.json`、内存 Map 快路径键加 dir 前缀、撞名仍 `collisionFreePath`；desktop typecheck + pre-push turbo typecheck 全绿，`octo-upload-inject` 11 用例过，待内网验证）；**v7.2 判据健壮性**：PR #424（判据止血，已合入 dev）+ 后续 refactor PR（布局收敛 + refresh 解耦；typecheck 12/12、insight 单测 14 pass，待提）；**v8 去掉预览面板第四栏已实现**（UXAI 分支 `refactor/insight-drop-preview-pane`：删 `preview-pane.tsx` + store 的 `previewFile` 字段 + 单击改开 tab + 下载改 IPC 优先；新增 `open-in-tab.test.ts` 覆盖 W1–W3 共 27 用例，insight 单测 149 pass / 36 既有失败不增，仓库根 typecheck 12/12、`packages/app` vite build 过；**W4–W11 手工验证待跑**，待提 PR）。**连带发现并补救**：SPEC-INS-026 的 #483–#485 因串行 PR 合并顺序反了（底座 #482 先进 dev，上面三层才落到底座分支）**内容从未进入 dev**，四个 PR 却都显示 MERGED——补合 PR #505（文本零冲突，仅 `action-bar.tsx` 一处语义冲突：dev 侧新增的归档链路还留着已退役的 `case "mindmap"`/`case "table"`；typecheck 12/12、insight 单测 169 pass/36）|
| A0 | `[—]` | **ADR-015 文件传参架构**[adr/015](docs/adr/015-file-passing-architecture.md) — 按「文件类×用途」分流（文本=FilePart内联 / office=路径+extract / 图片=S3 URL / MCP=handle+按需上传）；决策「有存储走 S3 URL 不走 base64」 | — | 草案已立 |
| A1 | `[M]` | **文件传参机制（Agent 正常化）**[SPEC-INS-015](docs/specs/infra/insight-file-passing.md) — 按 ADR-015 四分支落地：txt/md→FilePart 内联、office→extract_document、图片→vision、任意→MCP 按需上传；插件改文件名/路径匹配、模型不碰 URL。**含 A2**（图片 vision 一并做了） | A | **已实现，内网验证中**（UXAI PR #251 `feat/insight-file-passing`：①③④ 已验；② office 走 extract_document **stub**、正文抽取待 B）|
| A2 | `[S~M]` | **图片附件 → vision** — 已并入 A1 落地：change 即传 S3 + 缩略图 + 发送走 `FilePart{url}` vision。[spec](docs/specs/ui/insight-image-attachment.md) 收敛为「粘贴/剪贴板交互补充」 | A1 | **已实现（随 A1）** |
| B | `[S~M]` | **`extract_document` tool**[SPEC-INS-016](docs/specs/infra/insight-extract-document.md) — office(docx/pdf/xlsx/pptx)→文本（mammoth / unpdf / exceljs / jszip 直抽），顺带返回字数/token 估算（测量机制）。**office「模型读」硬依赖它**（见 ADR-015）。**v2（2026-08-06）全量落盘**：内网实测三四万字 docx 正文被腰斩、一轮 10 个文件必失败、分批读完后模型又从头重读——根因三条且都不在解析库（① 通用 Truncate 的 `MAX_BYTES=50KB` 是**字节**，中文 ≈1.7 万字就砍，且它是为「头部够用」的 bash/grep 设计的，套在「全文即目的」的抽取上语义就错；② 10 vs 5 是上下文窗口的除法题，与并发无关；③ 模型重读是因为 compaction 把正文压成了摘要——UI 显示的是数据库原始消息、模型看到的是重组后的上下文，**两者会分叉**）。改为**全量落盘 + 阈值分流**：正文一律落 `.octo/<sessionID>/extracted/<名>.md`，小文件额外内联全文（一式两份）、大文件只回元信息 + 路径 + 2000 字预览，工具自己保证永不触发通用兜底；强制软折行 ≤500 字符/行（`read` 的 `MAX_LINE_LENGTH=2000` 会砍超长行尾且**无续读手段**，不折行等于白落；兼顾 grep 行粒度）；撞名按首行 source 标记退到 `-2.md` 防静默串数据；落盘失败降级为整篇返回。**明确否掉**：按 heading 结构化分页（真实用研素材普遍无样式层级）、解析件进文件管理（SPEC-INS-014 v4「outputs 只收真·产物」，解析件是给模型看的中间件）、子代理分治（是通读类任务的正解，但需先解 registry gate，独立立项）。**已知边界**：落盘解决「工具不许丢数据」，不解决「全文塞不塞得下」——10 份文档做通读类任务仍会撞窗口 | A | **v1 已上线**（UXAI PR #272）；**v2 已实现（外网）**：`extract_document.ts` 改造 + 提示词 md/txt 同步 + 单测 14 pass（新增小文件一式两份 / 大文件只回预览 / 折行 / 撞名 / 落盘失败降级 5 组）、`tsgo --noEmit` 干净；**待内网验证**（三四万字 docx 首行字数与真实一致、模型能 grep 命中并 read 到文末、一轮 10 个文件全部抽取成功且各有落盘件） |
| B1 | `[M]` | **MCP 显式入口**[SPEC-INS-017](docs/specs/infra/insight-mcp-explicit-entry.md) — 输入框 chip 单 turn 触发（复用预置提示词机制）、MCP 工具（含未对接 `search_reports`）退出模型常驻工具集、MCP 仪式段落迁出常驻提示词、埋点（chip 点击率 = 本地线缺口仪表盘）。承接 ADR-015 2026-07-03 修订（分支 ④ 模型隐式 → 用户显式） | A1 | **已实现（外网）**（UXAI `feat/mcp-explicit-entry`，待提 PR / 内网联调；§3 采用方案 A turn 级 gate；§2.1 2026-07-06 收敛为「触发**范围**固定 + URL 传递固定」——chip 是纯常驻的范围限制，是否调用归模型判断，文件获取/分桶归模型对话；实现记录见 spec §8）；**2026-08-19 追加 §8-9**：内网反馈「观点提取时还会同时触发 `extract_document`、多文件即超限」——MCP 只收文件名、正文由服务端自解析，本地抽取对该调用零贡献纯占上下文，故 chip turn gate 增关 `extract_document` + 模板补「不要先读文件正文」纪律（只关 chip turn：后续轮次它仍是 office 唯一读取入口）；**2026-08-20 追加 §2.1「清单来源」**：MCP 文件白名单从只认 `[附件]` 扩到同时认 `[引用文件]`（SPEC-INS-023 的 `@` 引用），修「`@` 一个已生成的会话文件后无法触发 MCP 解析」——白名单语义不变，修的是 `@` 这条入口从未被登记进白名单的契约漏配 |
| B1.5 | `[S~M]` | **工具集收敛 + 权限交互 + extract 入口修正**[SPEC-INS-021](docs/specs/infra/insight-toolset-convergence.md)（2026-07-11 自 B2 拆出）— 现状核查：octo_insight 无白名单、全量 coding 工具集暴露（.md frontmatter `tools:` 不被代码消费）；「贴路径卡死」= external_directory ask 无 UI 静默阻塞。收敛白名单（权限层 deny bash/todowrite/生图；edit/apply_patch 因上游 EDIT_TOOLS 共键改走 registry 层裁剪,见 spec §1 落地记录；webfetch/websearch 保留给竞品分析线,chip turn gate 增关 webfetch）+ insight 接原生权限 Dock（external_directory 保持 ask）+ extract_document 解除「仅附件」措辞 / 支持 txt·md / office 禁走 read + frontmatter 清理 + 工具显示名人话化 | B1 | **实现已合入 dev**（032 在其基础上改 `extract_document` gate）：typecheck/单测/源码 server 配置核对通过;**待人工验证（外网 Dock 交互 + 内网复现,清单见 spec §8）**；**2026-08-19 §3.1 订正**：§3「提示词三条简化为两条」把「上传 txt→已内联、不用调」那条边界弄丢了，留下一句要模型自判「正文在不在我上下文里」的软句子——模型答不了（内联正文无文件名边界标记），保险起见就抽，导致同一份正文在上下文里存在**两份**（内联 + 工具回灌），多 txt 直接撑爆。判据改为确定性的「在不在 `[附件]` 区块」；本次只做提示词层，机制层硬拦（插件 `tool.execute.before`）先不做、按 `[octo:extract] ok` 的 `format` 字段观测漏网率再定 |
| B2 | `[M]` | **本地解析 v1（长上下文直喂）**[SPEC-INS-018](docs/specs/infra/insight-local-analysis-v1.md) — 前提已变（内网 GLM 5 128K）：观点解析先行,单文档直喂 + 多文档 task 子代理分治;能力形态定案（2026-07-11）：**零新增解析 tool、零新增用户入口**（对话即能力;本地/MCP 路线之争不暴露给用户,产品定案）,解析 = 模型会话内推理,MCP chip 维持 017 现状;输出契约以出处引用为一等公民（发言人 + 引语 + **段落锚点** + 源路径）+ **引用校验器**（落盘后代码级子串核验,失配响亮标注）;护栏降为建议话术;评测内网闭环（用研同学评分表） | A、B、B1、B1.5 | **草案已立（2026-07-11 修订）** |
| B2.5 | `[M]` | **子代理分治(多文档通读)+ 工具面声明化**[SPEC-INS-032](docs/specs/infra/insight-subagent-dispatch.md)（2026-08-18 立项,承接 016 §8 那条待办）— 解 016 v2 明写的已知边界「10 份文档做通读类任务仍会撞窗口」。形态:**一份文档 = 一个 `insight_reader` 子代理**,子代理自己调 `extract_document` 抽取 + read 通读、**只回传结论不写文件**,父代理收结论写报告;价值是**上下文隔离不是并发**,串行即可(20 份 3 万字:父上下文约 1.8 万 token)。**否掉**「父抽取 + 子只 read」（不用解 gate,但每份 2000 字预览是按份数线性增长的税,20 份 ≈ 4.2 万 token,把父上下文吃回去）。三处机制改动:① `extract_document` 的 gate 从 registry 按 agent 名硬编码改为**权限层声明**(defaults deny + 显式 allow)——上游 `tools:` 字段本就是废弃别名、会被 normalize 成 `permission`,故这不是新造一层;第三方 skill 自带 agent 可自助声明,不需我们发版;② task 撤销 021 的「turn 级默认关」改常驻放开,但用 `task: { insight_reader: deny/allow }` 把候选收敛到唯一子代理,**且不外溢到其他 agent**(其他 agent 的 task 工具本身不受影响);③ 工作区判据从「本会话 agent 名」改为「**会话树根会话**」,子代理的产物 / 解析件 / bash cwd / glob 默认范围一律归父会话目录（业界共识:子代理隔离的是上下文,不是文件系统）。UI:P0 维持内联单行 task 卡片 + 父代理逐份文字回执,**否掉** design 的「侧栏 subAgent 分组」;卡片增强(步数/耗时/展开结论)留 P1。**v2 增补(2026-08-21):入口覆盖面 + 读取契约** — v1 默认「多文档 = 二进制类」,只覆盖了 `extract_document` 那条路;md/txt 是原生可读格式,走的是「附件 → 合成 `read`」另一条链路,而 `read` 有 2000 行 / 50KB 硬上限,于是**单份两三万字中文已被静默截到前 ~1.7 万字**(正确性问题,不报错)、10 份累加照样撞窗口(容量问题)。定案:**发送前按总字节确定性分层**(≤`INLINE_BUDGET` 32KB 内联、行为不变;超了**整批**转分治,只给路径 + 字节 + 行数),口径取字节而非字数(与 read 同口径);**分治不扩容单份** —— 子代理与父同模型同窗口,>`SINGLE_DOC_LIMIT` 150KB 响亮失败,份内切段列后续;并补子代理读取契约:按任务给的**总行数**自查读完(替掉「读到 End of file」这种靠自觉的启发式)、结论带**可 grep 回原文的锚点**、**边读边记**。业界对照:分层是主流(claude.ai/ChatGPT),**否掉 RAG**(检索适合「找某句话」,通读型任务必漏,map-reduce 才是正解) | B（016 v2）、B1.5（021） | **实现已合入 dev**（`prompt/insight_reader.txt`、根会话判据已在 `octo-session-workdir.ts`）:agent/registry/workdir 插件/extract 落盘/前端 gate/提示词 + 单测(agent 3 组、registry 1 组、extract 1 组)全绿、双包 typecheck 干净;**待人工验证**（外网分治主路径 + 不外溢回归、内网弱模型遵从度,清单见 spec §10）。**v2 待实现**:前端分层判定 + 两个常量、两条提示词收紧、`build-prompt-parts.test.ts` 分层用例、`[octo:attach]` 两条日志;其中 **§10.2-8 埋点实测**(3 万字稿 80% 位置埋唯一事实,看子代理捞不捞得出来)的结果决定「份内切段分治」立不立项。清单见 spec §12.2 |
| C | `[S]` | **`@` 引用所有文档类型** — 文件名联想 + 按需读取（纯文本可先于 B、office 等 B 点亮）| A（office 靠 B）| 待起草 |
| D | `[M]` | **二次生成** — 读本地源 + 上轮产物（MCP 或本地）→ edit；含 **`[产物]` 清单回注**（outputs 产物路径注入 prompt,与 `[附件]` 对称）+ `edit` 工具 + 产物版本策略（不可变 + `-v2` 新文件,B2 §3 已定死）；独立于能力线 | A（office 源靠 B）、B1.5（read/write 已挂）、B2 | 待起草 |
| E | `[L]` | **本地解析能力线后续** — 观点解析之外的能力逐个加（思维导图 / …）;**块级拆分合并由 B1 埋点数据决策**（chip 点击中超阈值占比,上线 2–4 周回看）,不预先立项;方法论 / 体裁 skill 依赖 [skill-system](docs/specs/agents/skill-system.md)（分层：格式归 tool、体裁归 skill、编排归代码） | B1、B2 | 待数据（v1 收敛为 B2） |
