# Octo Agent 环境变量模板
#
# 用法：
#   cp .env.example .env.local
#   # 然后填入实际值；.env.local 被 git 默认忽略，不会进 commit
#
# Vite 规则：只有 VITE_ 前缀的变量会暴露给浏览器侧代码
# 详见对应 spec：docs/specs/infra/file-upload.md §端点

# ── Insight: 文件上传服务端点 ──
# 形态示例（占位，按内网实际地址替换）：
#   VITE_OCTO_UPLOAD_ENDPOINT=https://<intranet-host>/<path>/uploadServer/api/files
# 留空则客户端上传会抛 ENDPOINT_NOT_CONFIGURED
VITE_OCTO_UPLOAD_ENDPOINT=
